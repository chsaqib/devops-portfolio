{
  "name": "saqib-devops-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 4173",
    "check": "node --check dist/script.js && node --check worker.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.139.0"
  }
}
